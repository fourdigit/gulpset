sudo: false

language: node_js

cache:
  directories: [~/.npm]

notifications:
  email: false

node_js:
  - lts/carbon

install:
  - travis_retry yarn install

script:
  - node --version
  - npm --version
  - yarn --version
  - yarn build:prod

deploy:
  provider: npm
  email: vinhpq.tn@gmail.com
  api_key: e6309f85-6f7d-4458-9b96-52bb4f3c9ede
  on:
    all_branches: true